---
- name: Configure APT repositories
  ansible.builtin.deb822_repository:
    name: custom-repo
    types: deb
    uris: "{{ set_repository_apt_urls }}"
    suites: "{{ set_repository_repositories }}"
    components:
      - main
      - universe
    state: present
  when:
    - set_repository_apt_urls | default([]) | length > 0
    - set_repository_repositories | default([]) | length > 0
  register: set_repository_apt_custom_repositories
  notify: Update APT cache
